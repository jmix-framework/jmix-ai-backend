<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd"
        objectQuotingStrategy="QUOTE_ONLY_RESERVED_WORDS">
    <!-- Reverses 11-040000-retire-ambiguous-checks: the questions retired there are back in the
         suite. Their rewritten counterparts stay active as well, so overlapping topics are now
         probed by both wordings. -->
    <changeSet id="1" author="jmix-ai-backend">
        <update tableName="CHECK_DEF">
            <column name="ACTIVE" valueBoolean="true"/>
            <where>ACTIVE = false</where>
        </update>
    </changeSet>
    <!-- The only definition left inactive: its question text is byte-identical to the active
         0fab7a68 record, so running both would ask the same question twice in every run and
         count it twice in the averages. -->
    <changeSet id="2" author="jmix-ai-backend">
        <update tableName="CHECK_DEF">
            <column name="ACTIVE" valueBoolean="false"/>
            <where>ID = '579dca5a-d68b-41fd-beeb-0b91dfe9a6ae'</where>
        </update>
    </changeSet>
</databaseChangeLog>
